package com.civictracker.department_service.model;

// This enum will be the source of truth for issue statuses
public enum Status {
    RECEIVED,
    ASSIGNED,
    IN_PROGRESS,
    RESOLVED,
    CLOSED
}