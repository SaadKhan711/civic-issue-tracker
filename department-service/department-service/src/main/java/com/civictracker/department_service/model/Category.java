package com.civictracker.department_service.model;

// This enum must be kept in sync with the one in the issue-reporting-service
public enum Category {
    ROAD_WORKS,
    WATER_SUPPLY,
    SANITATION,
    STREET_LIGHTS,
    PARKS_AND_RECREATION,
    OTHER
}