server.port=8080

spring.datasource.url=jdbc:postgresql://localhost:5432/user_db
spring.datasource.username=postgres
spring.datasource.password=pass
spring.jpa.hibernate.ddl-auto=update

# =================================================================
# NEW SECTION: JWT Configuration
# =================================================================
# A strong, secret key for signing the JWT. In a real app, this MUST be in a secure vault, not here.
# This key MUST be at least 256 bits (32 bytes) long for HS256 algorithm.
jwt.secret=aVeryLongAndSecureSecretKeyForOurCivicTrackerApplication12345
# Token expiration time in milliseconds (e.g., 1 hour)
jwt.expiration.ms=3600000